# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=ums_event_pass

# JWT Secret (CHANGE THIS TO A RANDOM STRING!)
JWT_SECRET=your-super-secret-jwt-key-change-this-to-something-random

# Server Port
PORT=3001

# INSTRUCTIONS:
# 1. Copy this file to .env (remove .example)
# 2. Update DB_PASSWORD if your MySQL has a password
# 3. Generate a random JWT_SECRET (use a password generator)
# 4. Never commit the .env file to git!
